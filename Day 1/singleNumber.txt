class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int i=0;
        int x=0;
        while(i<nums.size()){
            x=x^nums[i];
            i++;
        }return x;

    }
};